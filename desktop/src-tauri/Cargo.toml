[package]
name = "voidx-desktop"
version = "1.0.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.1", features = [] }

[dependencies]
tauri = { version = "2.1", features = [] }
serde_json = "1"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
